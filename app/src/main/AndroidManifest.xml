<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- TV: declare leanback support but NOT required so phone APK still installs -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:name=".CalendarApplication"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.VamanitCalendar"
        tools:targetApi="31">

        <!-- ── Phone / default launcher ── -->
        <activity
            android:name=".ui.signin.SignInActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── TV / Leanback launcher alias (same activity) ── -->
        <activity-alias
            android:name=".TvEntryActivity"
            android:targetActivity=".ui.signin.SignInActivity"
            android:exported="true"
            android:banner="@drawable/tv_banner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateHidden" />

        <!--
            AppAuth redirect URI handler.
            Replace {PACKAGE_NAME} with com.vamanit.calendar
            The scheme must match what you register in Google Cloud Console.
        -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Google OAuth redirect — reverse of Desktop OAuth client ID -->
                <data
                    android:scheme="com.googleusercontent.apps.534654568144-qbbo6knmoqo3uqga35e0ipsq92d7dskl"
                    android:host="oauth2redirect" />
            </intent-filter>
        </activity>

        <!-- MSAL redirect URI handler for Microsoft auth (debug keystore SHA-1 base64) -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="msauth"
                    android:host="com.vamanit.calendar"
                    android:path="/RwD+B3WBhHQoDk59NqvfGvyHnUs=" />
            </intent-filter>
        </activity>

    </application>
</manifest>
